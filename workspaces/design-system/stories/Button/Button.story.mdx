import { useState } from 'react'
import { Meta } from '@storybook/addon-docs'
import { Button } from '../../src'
import { PreviewItem, PreviewGroup } from '../utils'

<Meta title="MDX/Button" />

# Button

<div style={{ display: 'flex', flexDirection: 'column', rowGap: 24 }}>
  <PreviewGroup>
    <PreviewItem label={'<Button />'}>
      <Button size="lg">Default</Button>
    </PreviewItem>
    <PreviewItem label={'&:hover, &:focus'}>
      <Button size="lg">Default</Button>
    </PreviewItem>
  </PreviewGroup>
  <PreviewGroup>
    <PreviewItem label={'<Button variant="outline" />'}>
      <Button size="lg" color="primary" variant="outline">
        Default
      </Button>
    </PreviewItem>
    <PreviewItem label={'&:hover, &:focus'}>
      <Button size="lg" color="primary" variant="outline">
        Default
      </Button>
    </PreviewItem>
  </PreviewGroup>
  <PreviewGroup>
    <PreviewItem label={'<Button variant="text" />'}>
      <Button size="lg" color="primary" variant="text">
        Default
      </Button>
    </PreviewItem>
    <PreviewItem label={'&:hover, &:focus'}>
      <Button size="lg" color="primary" variant="text">
        Default
      </Button>
    </PreviewItem>
  </PreviewGroup>
  <PreviewGroup>
    <PreviewItem label={'<Button disableShadow />'}>
      <Button size="lg" color="primary" disableShadow>
        Default
      </Button>
    </PreviewItem>
  </PreviewGroup>
  <PreviewGroup>
    <PreviewItem label={'<Button disabled />'}>
      <Button size="lg" disabled>
        Default
      </Button>
    </PreviewItem>
    <PreviewItem label={'<Button variant="text" disabled />'}>
      <Button size="lg" disabled variant="text">
        Default
      </Button>
    </PreviewItem>
  </PreviewGroup>
  <PreviewGroup>
    <PreviewItem label={'<Button color="primary" startIcon="local_grocery_store" />'}>
      <Button color="primary" startIcon="local_grocery_store" >
        Default
      </Button>
    </PreviewItem>
    <PreviewItem label={'<Button color="primary" endIcon="local_grocery_store" />'}>
      <Button color="primary" endIcon="local_grocery_store">
        Default
      </Button>
    </PreviewItem>
  </PreviewGroup>
</div>
